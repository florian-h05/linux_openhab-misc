# this conf enables https client certificate authentication, known as mutual TLS
# it can be either user in the server or the http block

ssl_client_certificate                    /config/nginx/mTLS/clientAuth_CA.crt;
ssl_crl                                   /config/nginx/mTLS/clientAuth_crl.pem;
ssl_verify_client                         on;
